# Core Django
Django==5.0.8
djangorestframework==3.15.2

# CORS support (frontend integration)
django-cors-headers==4.4.0

# Environment variables
python-dotenv==1.0.1

# PostgreSQL database driver
psycopg2-binary==2.9.9

# Authentication (JWT)
djangorestframework-simplejwt==5.3.1

# Password hashing utilities (Django already has but useful for extensions)
bcrypt==4.1.3

# WebSockets / Realtime (Django Channels)
channels==4.1.0
channels-redis==4.2.0

# Redis (required for Channels + Celery)
redis==5.0.8

# Background tasks
celery==5.4.0

# LiveKit server SDK (token generation for video calls)
livekit-api==0.7.0

# HTTP requests (for external services)
requests==2.32.3

# API schema/docs (optional but useful)
drf-spectacular==0.27.2
drf-spectacular
# Time utilities
pytz==2024.1